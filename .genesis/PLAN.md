# Implementation plan

> Generated from `.genesis/project.json`; do not edit this file.

- workflow: new-product
- phase: build
- plan approval: Milap Vaghasia at 2026-09-16T17:49:05.326Z

## Tasks

### T-1 — A runnable project skeleton exists: pinned dependencies in a local venv, one configuration point for model identifiers, credentials read only from the environment, and a corpus manifest that records a verified public source URL for every document

- state/risk: rejected / low
- requirements: FR-1, NFR-2, NFR-3, NFR-4, NFR-8
- scope: pyproject.toml, src, tests, corpus
- gates: scaffold: .venv/Scripts/python.exe -m pytest tests/test_config.py tests/test_manifest.py tests/test_store_roundtrip.py -q
- next: Run the task pre-flight.

### T-2 — Every page of every corpus document is classified as text, table or chart from PyMuPDF layout statistics alone, and the class plus the routing decision is recorded per page

- state/risk: rejected / medium
- requirements: FR-2, FR-4, NFR-5, AC-1, AC-12
- scope: src/ingest, tests
- gates: classifier: .venv/Scripts/python.exe -m pytest tests/test_classifier.py -q, independent-review: pending
- next: Run the task pre-flight.

### T-3 — Each page is extracted by the method matching its class: prose extraction for text, structure-preserving extraction for tables, Claude vision interpretation for charts, with the method used recorded per page

- state/risk: rejected / medium
- requirements: FR-3, FR-4
- scope: src/ingest, tests
- gates: extract: .venv/Scripts/python.exe -m pytest tests/test_extract.py -q, independent-review: pending
- next: Run the task pre-flight.

### T-4 — Extracted content is chunked without splitting tables mid-row, embedded by a local sentence-transformers model, and persisted to a local Chroma store that a fresh process can query, with unchanged documents skipped on re-ingest

- state/risk: rejected / medium
- requirements: FR-5, FR-6, FR-7, NFR-2, AC-2, AC-3
- scope: src/index, tests
- gates: index: .venv/Scripts/python.exe -m pytest tests/test_index.py -q, independent-review: pending
- next: Run the task pre-flight.

### T-5 — A query is answered by an agentic loop that plans sub-queries, searches, reflects on sufficiency, iterates with revised queries when evidence is insufficient, halts on sufficiency or an iteration or spend ceiling, and emits a machine-readable trace of the whole run

- state/risk: rejected / medium
- requirements: FR-8, FR-9, FR-10, NFR-1, AC-6
- scope: src/retrieval, tests
- gates: retrieval: .venv/Scripts/python.exe -m pytest tests/test_retrieval_loop.py -q, independent-review: pending
- next: Run the task pre-flight.

### T-6 — A router selects at most two of the four specialists per query and records a stated reason for every selection and non-selection, and each specialist answers only from retrieved corpus evidence within its own scope

- state/risk: rejected / medium
- requirements: FR-11, FR-12, FR-13, AC-7
- scope: src/agents, tests
- gates: router: .venv/Scripts/python.exe -m pytest tests/test_router.py -q, independent-review: pending
- next: Run the task pre-flight.

### T-7 — One CLI command turns an analyst question into a Markdown rationale note on disk that carries a structural citation on every material claim, surfaces rather than hides specialist disagreement, states the not-a-rating disclaimer, and reports specialists invoked, iterations and run cost

- state/risk: rejected / high
- requirements: FR-14, FR-15, FR-16, FR-17, FR-18, FR-19, NFR-7, AC-4, AC-10
- scope: src/report, src/cli.py, tests
- gates: note: .venv/Scripts/python.exe -m pytest tests/test_note.py -q, independent-review: pending
- next: Run the task pre-flight.

### T-8 — Three golden questions run end to end against the real corpus and the live API, each within the cost ceiling and the time limit, with citations verified against source PDFs and the repository clean of credentials

- state/risk: rejected / high
- requirements: NFR-1, NFR-6, AC-5, AC-8, AC-9, AC-11
- scope: tests, corpus, notes
- gates: acceptance: .venv/Scripts/python.exe -m pytest tests/test_acceptance.py -q, independent-review: pending
- next: Run the task pre-flight.

### T1 — A runnable project skeleton exists: pinned dependencies in a local venv, one configuration point for model identifiers, credentials read only from the environment, and a corpus manifest that records a verified public source URL for every document

- state/risk: done / low
- requirements: FR-1, NFR-2, NFR-3, NFR-4, NFR-8
- scope: pyproject.toml, src, tests, corpus
- gates: scaffold: ".venv/Scripts/python.exe" -m pytest tests/test_config.py tests/test_manifest.py tests/test_store_roundtrip.py -q
- next: Run the task pre-flight.

### T2 — Every page of every corpus document is classified as text, table or chart from PyMuPDF layout statistics alone, and the class plus the routing decision is recorded per page

- state/risk: done / medium
- requirements: FR-2, FR-4, NFR-5, AC-1, AC-12
- scope: src/risk_analytics, tests
- gates: classifier: ".venv/Scripts/python.exe" -m pytest tests/test_classifier.py -q, independent-review: pass
- next: Run the task pre-flight.

### T3 — Each page is extracted by the method matching its class: prose extraction for text, structure-preserving extraction for tables, Claude vision interpretation for charts, with the method used recorded per page

- state/risk: rejected / medium
- requirements: FR-3, FR-4
- scope: src/risk_analytics, tests
- gates: extract: ".venv/Scripts/python.exe" -m pytest tests/test_extract.py -q, independent-review: pending
- next: Run the task pre-flight.

### T4 — Extracted content is chunked without splitting tables mid-row, embedded by a local sentence-transformers model, and persisted to a local Chroma store that a fresh process can query, with unchanged documents skipped on re-ingest

- state/risk: rejected / medium
- requirements: FR-5, FR-6, FR-7, NFR-2, AC-2, AC-3
- scope: src/risk_analytics, tests, pyproject.toml
- gates: index: ".venv/Scripts/python.exe" -m pytest tests/test_index.py -q, independent-review: pending
- next: Run the task pre-flight.

### T5 — A query is answered by an agentic loop that plans sub-queries, searches, reflects on sufficiency, iterates with revised queries when evidence is insufficient, halts on sufficiency or an iteration or spend ceiling, and emits a machine-readable trace of the whole run

- state/risk: rejected / medium
- requirements: FR-8, FR-9, FR-10, NFR-1, AC-6
- scope: src/risk_analytics, tests
- gates: retrieval: ".venv/Scripts/python.exe" -m pytest tests/test_retrieval_loop.py -q, independent-review: pending
- next: Run the task pre-flight.

### T6 — A router selects at most two of the four specialists per query and records a stated reason for every selection and non-selection, and each specialist answers only from retrieved corpus evidence within its own scope

- state/risk: rejected / medium
- requirements: FR-11, FR-12, FR-13, AC-7
- scope: src/risk_analytics, tests
- gates: router: ".venv/Scripts/python.exe" -m pytest tests/test_router.py -q, independent-review: pending
- next: Run the task pre-flight.

### T7 — One CLI command turns an analyst question into a Markdown rationale note on disk that carries a structural citation on every material claim, surfaces rather than hides specialist disagreement, states the not-a-rating disclaimer, and reports specialists invoked, iterations and run cost

- state/risk: rejected / high
- requirements: FR-14, FR-15, FR-16, FR-17, FR-18, FR-19, NFR-7, AC-4, AC-10
- scope: src/risk_analytics, tests
- gates: note: ".venv/Scripts/python.exe" -m pytest tests/test_note.py -q, independent-review: pending
- next: Run the task pre-flight.

### T8 — Three golden questions run end to end against the real corpus and the live API, each within the cost ceiling and the time limit, with citations verified against source PDFs and the repository clean of credentials

- state/risk: rejected / high
- requirements: NFR-1, NFR-6, AC-5, AC-8, AC-9, AC-11
- scope: tests, corpus, notes
- gates: acceptance: ".venv/Scripts/python.exe" -m pytest tests/test_acceptance.py -q, independent-review: pending
- next: Run the task pre-flight.

### T3A — Both corpus documents are registered in corpus/manifest.json with verified public source URLs, the PDFs are excluded from version control, and every document the pipeline reads is resolved through the manifest rather than a hardcoded path

- state/risk: done / low
- requirements: FR-1, NFR-3, AC-11
- scope: corpus, .gitignore, src/risk_analytics, tests
- gates: corpus: ".venv/Scripts/python.exe" -m pytest tests/test_manifest.py tests/test_corpus_registration.py -q
- next: Run the task pre-flight.

### T4A — Text pages are extracted as prose and table pages with their row and column structure preserved, each page recording the method actually used; chart pages are recorded and skipped without any model call

- state/risk: done / medium
- requirements: FR-3, FR-4
- scope: src/risk_analytics, tests
- gates: extract: ".venv/Scripts/python.exe" -m pytest tests/test_extract.py -q, independent-review: pass
- next: Run the task pre-flight.

### T5A — Extracted content is chunked without splitting a table mid-row, embedded by a local sentence-transformers model, and persisted to a local Chroma store a fresh process can query, with unchanged documents skipped on re-ingest

- state/risk: done / medium
- requirements: FR-5, FR-6, FR-7, NFR-2, AC-2, AC-3
- scope: src/risk_analytics, tests, pyproject.toml
- gates: index: ".venv/Scripts/python.exe" -m pytest tests/test_index.py -q, independent-review: pass
- next: Run the task pre-flight.

### T6A — A query is answered by an agentic loop that plans sub-queries, searches, reflects on whether the evidence suffices, revises and retries when it does not, halts on sufficiency or an iteration or spend ceiling, and emits a machine-readable trace of the whole run

- state/risk: done / medium
- requirements: FR-8, FR-9, FR-10, NFR-1, AC-6
- scope: src/risk_analytics, tests
- gates: retrieval: ".venv/Scripts/python.exe" -m pytest tests/test_retrieval_loop.py -q, independent-review: pass
- next: Run the task pre-flight.

### T7A — A router selects at most two of the four specialists per query and records a stated reason for every selection and non-selection, and each specialist answers only from retrieved corpus evidence, reporting insufficient evidence rather than filling a gap from general knowledge

- state/risk: active / medium
- requirements: FR-11, FR-12, FR-13, AC-7
- scope: src/risk_analytics, tests
- gates: router: ".venv/Scripts/python.exe" -m pytest tests/test_router.py -q, independent-review: pending
- next: Run the task pre-flight.

### T8A — One CLI command turns an analyst question into a Markdown rationale note on disk carrying a structural citation on every material claim, surfacing rather than hiding specialist disagreement, stating the not-a-rating disclaimer, and reporting specialists invoked, iterations and run cost

- state/risk: queued / high
- requirements: FR-14, FR-15, FR-16, FR-17, FR-18, FR-19, NFR-7, AC-4, AC-10
- scope: src/risk_analytics, tests
- gates: note: ".venv/Scripts/python.exe" -m pytest tests/test_note.py -q, independent-review: pending
- next: Run the task pre-flight.

### T9A — The three golden questions run end to end against the real corpus and the live API, each inside the cost ceiling and the time limit, with every citation verified by hand against the source PDFs and the repository clean of credentials

- state/risk: queued / high
- requirements: NFR-1, NFR-6, AC-5, AC-8, AC-9, AC-11
- scope: tests, corpus, notes
- gates: acceptance: ".venv/Scripts/python.exe" -m pytest tests/test_acceptance.py -q, independent-review: pending
- next: Run the task pre-flight.

